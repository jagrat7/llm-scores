/** One source for the product name, so the wordmark and document title never drift. */
export const APP_NAME = "LLM Graphs"
export const APP_DESCRIPTION = "compare language models across benchmarks"
