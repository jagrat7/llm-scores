[project]
name = "deepswe-score-chart"
version = "0.1.0"
description = "Reproduction of the DeepSWE score versus task cost chart"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.3",
    "python-dotenv>=1.1.1",
    "seaborn>=0.13.2",
]

[tool.uv]
package = false
